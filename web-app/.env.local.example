# API 설정
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Mock API 사용 여부 (백엔드 서버 없이 테스트 시)
# true로 설정하면 하드코딩된 테스트 계정으로 로그인 가능
NEXT_PUBLIC_USE_MOCK_API=true

# 프로덕션 환경에서는 반드시 false로 설정
# NEXT_PUBLIC_USE_MOCK_API=false
